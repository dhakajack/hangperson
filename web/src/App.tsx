import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="intro">
        <p className="eyebrow">Web prototype</p>
        <h1>Hangperson</h1>
        <p>
          React, TypeScript, and Vitest are ready. The first slice ports the core game
          logic and client-side word-list parsing while the desktop app stays untouched.
        </p>
      </section>
    </main>
  )
}

export default App
